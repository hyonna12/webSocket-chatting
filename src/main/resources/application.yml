server:
  port: 8000
  servlet:
    context-path: /

spring:
  devtools:
    livereload:
      enabled: true
    restart:
      enabled: false
  freemarker:
    cache: false

logging:
  level:
    "[org.hibernate.SQL]": DEBUG
    #root: DEBUG
